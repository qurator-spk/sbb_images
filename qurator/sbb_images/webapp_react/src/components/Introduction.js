//Is it necessary????
import React from 'react';

const Introduction = () => {
  return (
    <div className="intro-text">
      <p>This is an AI-based image search tool which allows you to search for images inside documents from the Digitized Collections of the Berlin State Library (SBB). You can start your search by either by uploading an image, describing the image you are looking for, using a document’s unique identifier (PPN) to find the images inside a specific work, or by exploring our random image selection below. All the search results will be images. 
The underlying technology is open source and actively being developed by SBB in the Mensch.Maschine.Kultur project.</p>
    </div>
  );
};

export default Introduction;
